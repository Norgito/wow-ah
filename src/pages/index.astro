---
import '../styles/global.css';
---

<style is:global>
	@import url('https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@300;400;500;600;700&family=Russo+One&display=swap');

	:root {
		--color-primary: #7C3AED;
		--color-on-primary: #FFFFFF;
		--color-secondary: #A78BFA;
		--color-on-secondary: #0F172A;
		--color-accent: #F43F5E;
		--color-on-accent: #000000;
		--color-background: #0F0F23;
		--color-foreground: #E2E8F0;
		--color-card: #1E1C35;
		--color-card-foreground: #E2E8F0;
		--color-muted: #27273B;
		--color-muted-foreground: #94A3B8;
		--color-border: #4C1D95;
		--color-destructive: #EF4444;
		--color-on-destructive: #000000;
		--color-ring: #7C3AED;
	}
	html {
		font-family: 'Chakra Petch', system-ui, sans-serif;
		background: var(--color-background);
		color: var(--color-foreground);
	}
	h1, h2, h3, h4, h5, h6, .font-heading {
		font-family: 'Russo One', sans-serif;
	}
	
	/* Custom slim scrollbar for table cells */
	.custom-scrollbar::-webkit-scrollbar {
		width: 4px;
	}
	.custom-scrollbar::-webkit-scrollbar-track {
		background: rgba(30, 41, 59, 0.5); 
		border-radius: 4px;
	}
	.custom-scrollbar::-webkit-scrollbar-thumb {
		background: rgba(148, 163, 184, 0.3); 
		border-radius: 4px;
	}
	.custom-scrollbar::-webkit-scrollbar-thumb:hover {
		background: rgba(148, 163, 184, 0.5); 
	}
</style>

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>WoW Cross-Realm BoE Dashboard</title>
	</head>
	<body class="bg-[var(--color-background)] text-[var(--color-foreground)] min-h-screen">
		<main class="container mx-auto px-4 py-8 max-w-6xl">
			<header class="mb-8 border-b border-[var(--color-border)] pb-4">
				<h1 class="text-5xl font-heading text-[var(--color-primary)] drop-shadow-[0_0_10px_rgba(124,58,237,0.8)] tracking-wide mb-2 uppercase">World of Warcraft</h1>
				<h2 class="text-2xl text-[var(--color-foreground)] mt-1 font-heading tracking-wide">Cross-Realm BoE Dashboard</h2>
				<p class="text-[var(--color-muted-foreground)] mt-2">Find the cheapest raid gear and Bind-on-Equip items across multiple realms.</p>
			</header>

			<!-- Controls -->
			<div class="flex flex-col md:flex-row gap-4 mb-6">
				<div class="flex-1 flex gap-2 relative">
					<input 
						type="text" 
						id="search" 
						placeholder="E.g. Signet, Pendant..." 
						class="w-full bg-[var(--color-muted)] border border-[var(--color-border)] rounded-md pl-4 pr-24 py-2 text-[var(--color-foreground)] focus:outline-none focus:ring-2 focus:ring-[var(--color-ring)] shadow-sm transition-all duration-300"
						autocomplete="off"
					/>
					<!-- Suggestions Dropdown -->
					<ul id="suggestions" class="absolute z-50 left-0 right-0 top-full mt-1.5 bg-[var(--color-card)]/95 backdrop-blur-md border border-[var(--color-border)] rounded-lg shadow-[0_12px_30px_rgba(0,0,0,0.7)] hidden max-h-80 overflow-y-auto divide-y divide-[var(--color-border)]/40">
					</ul>
					<button id="search-btn" class="absolute inset-y-0 right-0 px-6 bg-[var(--color-primary)] text-[var(--color-on-primary)] font-heading tracking-wide rounded-r-md transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-[var(--color-ring)] hover:shadow-[0_0_15px_rgba(124,58,237,0.8)] hover:brightness-110">
						SEARCH
					</button>
				</div>
				<button id="favorites-btn" class="w-full md:w-auto px-5 py-2 bg-[var(--color-muted)] hover:bg-[var(--color-card)] text-[var(--color-accent)] border border-[var(--color-border)] font-heading tracking-wide rounded-md transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-[var(--color-ring)] flex items-center justify-center gap-2 hover:shadow-[0_0_15px_rgba(244,63,94,0.4)]">
					<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path></svg>
					FAVORITES
				</button>
				<button id="bestsellers-btn" class="w-full md:w-auto px-5 py-2 bg-[var(--color-muted)] hover:bg-[var(--color-card)] text-amber-400 border border-amber-500/40 font-heading tracking-wide rounded-md transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-amber-400 flex items-center justify-center gap-2 hover:shadow-[0_0_15px_rgba(245,158,11,0.4)]">
					<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.879 16.121A3 3 0 1012.015 11L11 14H9c0 .768.293 1.536.879 2.121z" /></svg>
					BEST SELLERS
				</button>
				<div class="w-full md:w-auto flex flex-col sm:flex-row gap-2">
					<select id="expansion" class="w-full sm:w-48 bg-[var(--color-muted)] border border-[var(--color-border)] rounded-md px-4 py-2 text-[var(--color-foreground)] focus:outline-none focus:ring-2 focus:ring-[var(--color-ring)] shadow-sm transition-colors cursor-pointer">
						<option value="all">All Expansions</option>
						<option value="Midnight">Midnight</option>
						<option value="The War Within">The War Within</option>
						<option value="Dragonflight">Dragonflight</option>
						<option value="Shadowlands">Shadowlands</option>
						<option value="Battle for Azeroth">Battle for Azeroth</option>
						<option value="Legion">Legion</option>
						<option value="Warlords of Draenor">Warlords of Draenor</option>
						<option value="Mists of Pandaria">Mists of Pandaria</option>
						<option value="Cataclysm">Cataclysm</option>
						<option value="Wrath of the Lich King">Wrath of the Lich King</option>
						<option value="The Burning Crusade">The Burning Crusade</option>
						<option value="Classic">Classic</option>
					</select>
					<select id="realm" class="w-full sm:w-48 bg-[var(--color-muted)] border border-[var(--color-border)] rounded-md px-4 py-2 text-[var(--color-foreground)] focus:outline-none focus:ring-2 focus:ring-[var(--color-ring)] shadow-sm transition-colors cursor-pointer">
						<option value="all">All Realms</option>
						<option value="1136">Aegwynn</option>
						<option value="1426">Aerie Peak</option>
						<option value="1129">Agamaggan</option>
						<option value="106">Aggramar</option>
						<option value="1070">Alexstrasza</option>
						<option value="52">Alleria</option>
						<option value="3676">Area 52</option>
						<option value="75">Argent Dawn</option>
						<option value="69">Arthas</option>
						<option value="77">Azgalor</option>
						<option value="121">Azjol-Nerub</option>
						<option value="3209">Azralon</option>
						<option value="1190">Baelgun</option>
						<option value="3723">Barthilas</option>
						<option value="54">Blackhand</option>
						<option value="73">Bleeding Hollow</option>
						<option value="64">Bloodhoof</option>
						<option value="117">Bronzebeard</option>
						<option value="3721">Caelestrasz</option>
						<option value="1168">Cairne</option>
						<option value="1138">Chromaggus</option>
						<option value="3683">Dalaran</option>
						<option value="157">Dark Iron</option>
						<option value="120">Darkspear</option>
						<option value="154">Detheroc</option>
						<option value="115">Draenor</option>
						<option value="114">Dragonblight</option>
						<option value="84">Dragonmaw</option>
						<option value="1425">Drakkari</option>
						<option value="63">Durotan</option>
						<option value="100">Earthen Ring</option>
						<option value="47">Eitrigg</option>
						<option value="67">Elune</option>
						<option value="162">Emerald Dream</option>
						<option value="96">Eonar</option>
						<option value="118">Feathermoon</option>
						<option value="127">Firetree</option>
						<option value="3725">Frostmourne</option>
						<option value="3234">Gallywix</option>
						<option value="3207">Goldrinn</option>
						<option value="158">Greymane</option>
						<option value="53">Hellscream</option>
						<option value="3661">Hyjal</option>
						<option value="57">Illidan</option>
						<option value="155">Kalecgos</option>
						<option value="3693">Kel'Thuzad</option>
						<option value="3726">Khaz'goroth</option>
						<option value="9">Kil'jaeden</option>
						<option value="4">Kilrogg</option>
						<option value="1071">Kirin Tor</option>
						<option value="1147">Kul Tiras</option>
						<option value="3694">Lightbringer</option>
						<option value="99">Llane</option>
						<option value="163">Maelstrom</option>
						<option value="78">Magtheridon</option>
						<option value="3684">Mal'Ganis</option>
						<option value="104">Malygos</option>
						<option value="3675">Moon Guard</option>
						<option value="3208">Nemesis</option>
						<option value="5">Proudmoore</option>
						<option value="1428">Quel'Thalas</option>
						<option value="1427">Ragnaros</option>
						<option value="1072">Ravencrest</option>
						<option value="1151">Rexxar</option>
						<option value="151">Runetotem</option>
						<option value="76">Sargeras</option>
						<option value="1185">Sen'jin</option>
						<option value="125">Shadow Council</option>
						<option value="12">Silver Hand</option>
						<option value="86">Silvermoon</option>
						<option value="160">Staghelm</option>
						<option value="60">Stormrage</option>
						<option value="58">Stormreaver</option>
						<option value="113">Suramar</option>
						<option value="3678">Thrall</option>
						<option value="11">Tichondrius</option>
						<option value="1175">Trollbane</option>
						<option value="3685">Turalyon</option>
						<option value="1184">Vek'nilash</option>
						<option value="71">Warsong</option>
						<option value="55">Whisperwind</option>
						<option value="1171">Wyrmrest Accord</option>
						<option value="61">Zul'jin</option>
					</select>
				</div>
			</div>

			<!-- Best Sellers Toolbar -->
			<div id="bestsellers-controls" class="hidden flex-wrap items-center justify-between gap-4 p-4 mb-6 bg-[var(--color-card)]/90 border border-amber-500/30 rounded-lg shadow-[0_0_20px_rgba(245,158,11,0.15)] backdrop-blur-md">
				<div class="flex items-center gap-3">
					<span class="text-xs font-heading uppercase text-amber-400 tracking-wider flex items-center gap-2">
						<span class="inline-block w-2.5 h-2.5 rounded-full bg-amber-400 animate-pulse shadow-[0_0_8px_rgba(245,158,11,0.8)]"></span>
						TSM Regional Velocity
					</span>
					<span class="text-xs text-[var(--color-muted-foreground)] hidden sm:inline">• Live US Regional Turnover & Velocity</span>
				</div>
				<div class="flex flex-wrap items-center gap-3">
					<div class="flex items-center gap-2">
						<label for="bs-sort" class="text-xs text-[var(--color-muted-foreground)] uppercase font-heading">Sort by:</label>
						<select id="bs-sort" class="bg-[var(--color-muted)] border border-[var(--color-border)] rounded px-3 py-1.5 text-xs text-[var(--color-foreground)] focus:outline-none focus:ring-1 focus:ring-amber-400 cursor-pointer">
							<option value="turnover">💰 Daily Gold Turnover</option>
							<option value="volume">📦 Units Sold / Day</option>
							<option value="rate">⚡ Sale Rate % (Velocity)</option>
						</select>
					</div>
					<div class="flex items-center gap-2">
						<label for="bs-minprice" class="text-xs text-[var(--color-muted-foreground)] uppercase font-heading">Min Value:</label>
						<select id="bs-minprice" class="bg-[var(--color-muted)] border border-[var(--color-border)] rounded px-3 py-1.5 text-xs text-[var(--color-foreground)] focus:outline-none focus:ring-1 focus:ring-amber-400 cursor-pointer">
							<option value="0">All Items</option>
							<option value="10">≥ 10g</option>
							<option value="100">≥ 100g (Valuable)</option>
							<option value="1000">≥ 1,000g (High Value)</option>
							<option value="10000">≥ 10,000g (Luxury / Epics)</option>
						</select>
					</div>
				</div>
			</div>

			<!-- Loading State -->
			<div id="loading" class="hidden flex-col justify-center items-center py-16">
				<div class="animate-spin rounded-full h-12 w-12 border-b-2 border-t-2 border-[var(--color-primary)] drop-shadow-[0_0_10px_rgba(124,58,237,0.8)] mb-4"></div>
				<p class="text-[var(--color-primary)] font-heading tracking-widest text-sm animate-pulse">SCANNING...</p>
			</div>

			<!-- Error State -->
			<div id="error" class="hidden bg-[var(--color-destructive)]/20 border border-[var(--color-destructive)] text-[var(--color-foreground)] px-4 py-3 rounded-lg mb-6 backdrop-blur-sm shadow-[0_0_15px_rgba(239,68,68,0.3)]">
				<p class="font-heading tracking-wide uppercase text-[var(--color-destructive)] drop-shadow-[0_0_5px_rgba(239,68,68,0.8)]" id="error-title">Failed to load auctions.</p>
				<p class="text-sm mt-1" id="error-desc">Check your API setup.</p>
			</div>

			<!-- Table -->
			<div class="overflow-x-auto bg-[var(--color-card)] rounded-lg shadow-[0_0_30px_rgba(0,0,0,0.8)] border border-[var(--color-border)] hidden" id="table-container">
				<table class="w-full text-left text-sm whitespace-nowrap">
					<thead class="bg-[var(--color-muted)] border-b border-[var(--color-border)] text-[var(--color-foreground)]">
						<tr>
							<th colspan="3"></th>
							<th colspan="2" class="px-6 pt-4 pb-2">
								<div id="realm-legend" class="flex flex-wrap items-center justify-center gap-5 text-[10px] font-heading uppercase tracking-widest text-[var(--color-muted-foreground)]">
									<span class="text-[var(--color-foreground)] mr-1">Realm Population:</span>
									<span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full bg-[var(--color-destructive)] shadow-[0_0_5px_rgba(239,68,68,0.8)]"></span> Full</span>
									<span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full bg-orange-500 shadow-[0_0_5px_rgba(249,115,22,0.8)]"></span> High</span>
									<span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full bg-yellow-400 shadow-[0_0_5px_rgba(250,204,21,0.8)]"></span> Med</span>
									<span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full bg-green-500 shadow-[0_0_5px_rgba(34,197,94,0.8)]"></span> Low</span>
									<span class="flex items-center gap-1.5"><span class="w-2.5 h-2.5 rounded-full bg-blue-500 shadow-[0_0_5px_rgba(59,130,246,0.8)]"></span> New</span>
								</div>
							</th>
						</tr>
						<tr>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Item</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs cursor-pointer hover:text-[var(--color-primary)] hover:drop-shadow-[0_0_5px_rgba(124,58,237,0.8)] select-none group transition-all" onclick="setSort('ilvl')">
								iLvl <span id="sort-icon-ilvl" class="text-[var(--color-primary)] opacity-0 transition-opacity">▼</span>
							</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Qty</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs cursor-pointer hover:text-[var(--color-primary)] hover:drop-shadow-[0_0_5px_rgba(124,58,237,0.8)] select-none group transition-all" onclick="setSort('minBuyout')">
								Buy (Cheapest) <span id="sort-icon-minBuyout" class="text-[var(--color-primary)]">▲</span>
							</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs cursor-pointer hover:text-[var(--color-destructive)] hover:drop-shadow-[0_0_5px_rgba(239,68,68,0.8)] select-none group transition-all" onclick="setSort('maxBuyout')">
								Sell (Most Exp.) <span id="sort-icon-maxBuyout" class="text-[var(--color-destructive)] opacity-0 transition-opacity">▼</span>
							</th>
						</tr>
					</thead>
					<tbody id="table-body" class="divide-y divide-[var(--color-border)]">
						<!-- Rows will be injected here via JavaScript -->
					</tbody>
				</table>
			</div>

			<!-- Best Sellers Table -->
			<div class="overflow-x-auto bg-[var(--color-card)] rounded-lg shadow-[0_0_30px_rgba(0,0,0,0.8)] border border-amber-500/30 hidden" id="bestsellers-container">
				<table class="w-full text-left text-sm whitespace-nowrap">
					<thead class="bg-[var(--color-muted)] border-b border-[var(--color-border)] text-[var(--color-foreground)]">
						<tr>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs w-16 text-center">Rank</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Item</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Region MV / Avg</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Units / Day</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs">Sale Velocity</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs text-amber-400">Est. Daily Turnover</th>
							<th class="px-6 py-4 font-heading uppercase tracking-wider text-xs text-right">Action</th>
						</tr>
					</thead>
					<tbody id="bestsellers-body" class="divide-y divide-[var(--color-border)]">
						<!-- Best sellers rows will be injected here via JavaScript -->
					</tbody>
				</table>
			</div>
			
			<!-- Empty State -->
			<div id="empty-state" class="flex flex-col items-center justify-center py-16 text-[var(--color-muted-foreground)]">
				<svg class="w-16 h-16 mb-4 text-[var(--color-border)] drop-shadow-[0_0_10px_rgba(76,29,149,0.5)]" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
				<p class="font-heading tracking-widest text-lg uppercase text-[var(--color-foreground)]" id="empty-title">Search for a BoE item</p>
				<p class="text-sm mt-1" id="empty-desc">Enter an item name to scan across multiple realms.</p>
			</div>
		</main>

		<script>
			let auctions = [];
			let favorites = JSON.parse(localStorage.getItem('wow_ah_favorites') || '[]');
			let isFavoritesView = false;
			let isBestSellersView = false;
			let currentSort = 'minBuyout';
			let sortDirection = 'asc';

			window.setSort = function(field) {
				if (currentSort === field) {
					sortDirection = sortDirection === 'asc' ? 'desc' : 'asc';
				} else {
					currentSort = field;
					sortDirection = field === 'ilvl' ? 'desc' : 'asc';
				}

				document.getElementById('sort-icon-ilvl').textContent = currentSort === 'ilvl' ? (sortDirection === 'asc' ? '▲' : '▼') : '▼';
				document.getElementById('sort-icon-ilvl').classList.toggle('opacity-0', currentSort !== 'ilvl');
				
				document.getElementById('sort-icon-minBuyout').textContent = currentSort === 'minBuyout' ? (sortDirection === 'asc' ? '▲' : '▼') : '▼';
				document.getElementById('sort-icon-minBuyout').classList.toggle('opacity-0', currentSort !== 'minBuyout');

				document.getElementById('sort-icon-maxBuyout').textContent = currentSort === 'maxBuyout' ? (sortDirection === 'asc' ? '▲' : '▼') : '▼';
				document.getElementById('sort-icon-maxBuyout').classList.toggle('opacity-0', currentSort !== 'maxBuyout');

				renderTable();
			};
			
			const searchInput = document.getElementById('search');
			const searchBtn = document.getElementById('search-btn');
			const favoritesBtn = document.getElementById('favorites-btn');
			const bestsellersBtn = document.getElementById('bestsellers-btn');
			const bestsellersControls = document.getElementById('bestsellers-controls');
			const bestsellersContainer = document.getElementById('bestsellers-container');
			const bestsellersBody = document.getElementById('bestsellers-body');
			const bsSortSelect = document.getElementById('bs-sort');
			const bsMinPriceSelect = document.getElementById('bs-minprice');
			const realmSelect = document.getElementById('realm');
			const expansionSelect = document.getElementById('expansion');
			const tableBody = document.getElementById('table-body');
			const tableContainer = document.getElementById('table-container');
			const loading = document.getElementById('loading');
			const errorMsg = document.getElementById('error');
			const errorDesc = document.getElementById('error-desc');
			const emptyState = document.getElementById('empty-state');
			const emptyTitle = document.getElementById('empty-title');
			const emptyDesc = document.getElementById('empty-desc');

			function formatCompactNumber(num) {
				if (!num && num !== 0) return '-';
				if (num >= 1000000) return (num / 1000000).toFixed(1) + 'M';
				if (num >= 1000) return (num / 1000).toFixed(1) + 'k';
				return Math.round(num).toLocaleString();
			}

			function formatMoney(copper) {
				if (!copper) return '-';
				const g = Math.floor(copper / 10000);
				
				if (g > 0) return `<span class="text-yellow-400 font-heading drop-shadow-[0_0_5px_rgba(250,204,21,0.5)] tracking-wide">${g.toLocaleString()}g</span>`;
				return `<span class="text-[var(--color-muted-foreground)] font-heading tracking-wide">< 1g</span>`;
			}

			function renderTsmBadge(tsm) {
				if (!tsm) return '';
				let badgeHtml = '';
				
				// Deal badge: if cheapest buyout is significantly lower than region market value
				if (tsm.dealDiscount && tsm.dealDiscount >= 15) {
					badgeHtml += `
						<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-heading uppercase tracking-wide bg-emerald-950/90 border border-emerald-500/60 text-emerald-400 shadow-[0_0_10px_rgba(16,185,129,0.3)]" title="Cheapest realm is ${tsm.dealDiscount}% cheaper than the US Region Market Value (${Math.round(tsm.marketValue).toLocaleString()}g)">
							🏷️ Deal: -${tsm.dealDiscount}% vs Region MV
						</span>
					`;
				}

				// Velocity & Demand badge
				if (tsm.saleRate >= 0.5 || tsm.soldPerDay >= 1000) {
					badgeHtml += `
						<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-heading uppercase tracking-wide bg-amber-950/80 border border-amber-500/50 text-amber-400" title="${Math.round(tsm.saleRate * 100)}% sale rate • ~${formatCompactNumber(tsm.soldPerDay)} sold per day across US realms">
							🔥 Hot Seller • ${Math.round(tsm.saleRate * 100)}% rate • ${formatCompactNumber(tsm.soldPerDay)}/day
						</span>
					`;
				} else if (tsm.saleRate >= 0.15) {
					badgeHtml += `
						<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-heading uppercase tracking-wide bg-purple-950/80 border border-purple-500/40 text-purple-300" title="${Math.round(tsm.saleRate * 100)}% sale rate • ~${formatCompactNumber(tsm.soldPerDay)} sold per day">
							⚡ Steady • ${Math.round(tsm.saleRate * 100)}% rate • ${formatCompactNumber(tsm.soldPerDay)}/day
						</span>
					`;
				} else if (tsm.saleRate > 0) {
					badgeHtml += `
						<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-heading uppercase tracking-wide bg-slate-800/80 border border-slate-700/50 text-slate-400" title="${Math.round(tsm.saleRate * 100)}% sale rate">
							❄️ Slow Mover (${Math.round(tsm.saleRate * 100)}%)
						</span>
					`;
				}

				return badgeHtml ? `<div class="flex flex-wrap gap-1.5 mt-2">${badgeHtml}</div>` : '';
			}
			
			function renderRealmTag(realmString) {
				const match = realmString.match(/^\[(.*?)\] (.*)$/);
				if (!match) return `<span class="text-[11px] text-[var(--color-ring)] ml-3 truncate max-w-[120px]" title="${realmString}">${realmString}</span>`;
				
				const pop = match[1];
				const name = match[2];
				
				let colorClass = 'bg-[var(--color-ring)] shadow-[0_0_5px_rgba(124,58,237,0.8)]';
				if (pop === 'Full') colorClass = 'bg-[var(--color-destructive)] shadow-[0_0_5px_rgba(239,68,68,0.8)]';
				else if (pop === 'High') colorClass = 'bg-orange-500 shadow-[0_0_5px_rgba(249,115,22,0.8)]';
				else if (pop === 'Medium') colorClass = 'bg-yellow-400 shadow-[0_0_5px_rgba(250,204,21,0.8)]';
				else if (pop === 'Low') colorClass = 'bg-green-500 shadow-[0_0_5px_rgba(34,197,94,0.8)]';
				else if (pop === 'New' || pop === 'New Players') colorClass = 'bg-blue-500 shadow-[0_0_5px_rgba(59,130,246,0.8)]';

				return `
					<div class="flex items-center ml-3 flex-1 min-w-0 justify-end gap-2" title="${realmString}">
						<span class="w-2 h-2 rounded-full flex-shrink-0 ${colorClass}"></span>
						<span class="text-[11px] text-[var(--color-muted-foreground)] truncate hover:text-[var(--color-foreground)] transition-colors cursor-default text-right max-w-[80px]">${name}</span>
					</div>
				`;
			}

			function renderRealmList(realms) {
				if (!realms || realms.length === 0) {
					return '<span class="text-[var(--color-muted-foreground)] font-heading text-sm">N/A</span>';
				}

				const top = realms[0];
				const rest = realms.slice(1);
				
				let html = `
					<div class="flex justify-between items-center text-sm pb-1.5">
						<span class="shrink-0">${formatMoney(top.price)}</span>
						${renderRealmTag(top.realmName)}
					</div>
				`;

				if (rest.length > 0) {
					html += `
						<button onclick="this.nextElementSibling.classList.toggle('hidden'); this.querySelector('.chevron').classList.toggle('rotate-180')" class="w-full text-left text-xs text-[var(--color-muted-foreground)] hover:text-yellow-400 transition-colors flex items-center justify-between py-1.5 mt-1 border-t border-[var(--color-border)] font-heading uppercase tracking-widest focus:outline-none">
							<span>+ ${rest.length} More</span>
							<span class="chevron transition-transform duration-200">▼</span>
						</button>
						<div class="hidden flex-col gap-1.5 mt-1 border-t border-[var(--color-border)]/50 pt-1.5">
							${rest.map(r => `
								<div class="flex justify-between items-center text-sm border-b border-[var(--color-border)]/50 last:border-0 pb-1 last:pb-0">
									<span class="shrink-0">${formatMoney(r.price)}</span>
									${renderRealmTag(r.realmName)}
								</div>
							`).join('')}
						</div>
					`;
				}
				
				return html;
			}

			function renderTable() {
				if (!tableContainer || !emptyState || !tableBody) return;

				if (auctions.length === 0) {
					tableContainer.classList.add('hidden');
					emptyTitle.textContent = "No items found";
					emptyDesc.textContent = "Try a different search term or change your filters.";
					emptyState.classList.remove('hidden');
					return;
				}

				emptyState.classList.add('hidden');
				tableContainer.classList.remove('hidden');

				const sortedAuctions = [...auctions].sort((a, b) => {
					let valA = a[currentSort];
					let valB = b[currentSort];
					
					if (currentSort === 'ilvl') {
						valA = parseInt(valA) || 0;
						valB = parseInt(valB) || 0;
					}
					
					if (valA < valB) return sortDirection === 'asc' ? -1 : 1;
					if (valA > valB) return sortDirection === 'asc' ? 1 : -1;
					return 0;
				});

				tableBody.innerHTML = sortedAuctions.map(auction => `
					<tr class="hover:bg-[var(--color-muted)] transition-all duration-300 group hover:shadow-[0_0_15px_rgba(124,58,237,0.15)] relative z-10 hover:z-20">
						<td class="px-6 py-4">
							<div class="flex items-center gap-4">
								<button class="favorite-toggle flex-shrink-0 text-[var(--color-muted-foreground)] hover:text-yellow-400 hover:drop-shadow-[0_0_8px_rgba(250,204,21,0.8)] focus:outline-none transition-all duration-300" data-id="${auction.id}">
									<svg class="w-6 h-6 ${favorites.includes(auction.id) ? 'text-yellow-400 fill-yellow-400 drop-shadow-[0_0_8px_rgba(250,204,21,0.8)]' : ''}" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
									</svg>
								</button>
								${auction.icon 
									? `<img src="${auction.icon}" alt="${auction.name}" class="w-10 h-10 rounded border border-[var(--color-border)] shadow-sm group-hover:border-[var(--color-primary)] group-hover:shadow-[0_0_10px_rgba(124,58,237,0.5)] transition-all duration-300" />` 
									: `<div class="w-10 h-10 bg-[var(--color-background)] rounded border border-[var(--color-border)] flex items-center justify-center shadow-sm"><span class="text-xs text-[var(--color-muted-foreground)]">?</span></div>`
								}
								<div class="flex flex-col min-w-0">
									<span class="font-heading tracking-wide text-[var(--color-foreground)] group-hover:text-[var(--color-primary)] cursor-pointer transition-colors">${auction.name}</span>
									<span class="text-xs text-[var(--color-muted-foreground)] mt-0.5">${auction.expansion}</span>
									${renderTsmBadge(auction.tsmStats)}
								</div>
							</div>
						</td>
						<td class="px-6 py-4 text-yellow-400 font-heading drop-shadow-[0_0_5px_rgba(250,204,21,0.5)] tracking-wide">${auction.ilvl}</td>
						<td class="px-6 py-4 text-[var(--color-foreground)] font-heading tracking-wide">${auction.quantity || 1}</td>
						<td class="px-6 py-4 align-top">
							<div class="flex flex-col">
								${renderRealmList(auction.top10Cheapest)}
							</div>
						</td>
						<td class="px-6 py-4 align-top">
							<div class="flex flex-col">
								${renderRealmList(auction.top10Expensive)}
							</div>
						</td>
					</tr>
				`).join('');

				// Attach favorite toggle handlers
				document.querySelectorAll('.favorite-toggle').forEach(btn => {
					btn.addEventListener('click', (e) => {
						const id = btn.getAttribute('data-id');
						if (favorites.includes(id)) {
							favorites = favorites.filter(fid => fid !== id);
						} else {
							favorites.push(id);
						}
						localStorage.setItem('wow_ah_favorites', JSON.stringify(favorites));
						
						// If we're in favorites view and unfavorited an item, remove it instantly from the screen
						if (isFavoritesView && !favorites.includes(id)) {
							auctions = auctions.filter(a => a.id !== id);
						}
						renderTable();
					});
				});
			}

			function setActiveView(view) {
				isFavoritesView = view === 'favorites';
				isBestSellersView = view === 'bestsellers';

				if (isBestSellersView) {
					bestsellersBtn.classList.add('ring-2', 'ring-amber-400', 'bg-amber-500/20');
					favoritesBtn.classList.remove('ring-2', 'ring-[var(--color-accent)]', 'bg-[var(--color-card)]');
					bestsellersControls.classList.remove('hidden');
					bestsellersControls.classList.add('flex');
					tableContainer.classList.add('hidden');
				} else if (isFavoritesView) {
					favoritesBtn.classList.add('ring-2', 'ring-[var(--color-accent)]', 'bg-[var(--color-card)]');
					bestsellersBtn.classList.remove('ring-2', 'ring-amber-400', 'bg-amber-500/20');
					bestsellersControls.classList.add('hidden');
					bestsellersControls.classList.remove('flex');
					bestsellersContainer.classList.add('hidden');
				} else {
					bestsellersBtn.classList.remove('ring-2', 'ring-amber-400', 'bg-amber-500/20');
					favoritesBtn.classList.remove('ring-2', 'ring-[var(--color-accent)]', 'bg-[var(--color-card)]');
					bestsellersControls.classList.add('hidden');
					bestsellersControls.classList.remove('flex');
					bestsellersContainer.classList.add('hidden');
				}
			}

			async function fetchBestSellers() {
				setActiveView('bestsellers');
				emptyState.classList.add('hidden');
				tableContainer.classList.add('hidden');
				bestsellersContainer.classList.add('hidden');
				errorMsg.classList.add('hidden');
				loading.classList.remove('hidden');
				loading.classList.add('flex');

				const sort = bsSortSelect.value;
				const minPrice = bsMinPriceSelect.value;

				try {
					const response = await fetch(`/api/best-sellers?sort=${sort}&minPrice=${minPrice}&limit=60`);
					const data = await response.json();

					if (!response.ok) {
						throw new Error(data.error || 'Failed to fetch best sellers');
					}

					renderBestSellers(data);
				} catch (err) {
					errorDesc.textContent = err.message;
					errorMsg.classList.remove('hidden');
					console.error('Failed to fetch best sellers', err);
				} finally {
					loading.classList.add('hidden');
					loading.classList.remove('flex');
				}
			}

			function renderBestSellers(items) {
				if (!items || items.length === 0) {
					bestsellersContainer.classList.add('hidden');
					emptyTitle.textContent = "No Best Sellers Found";
					emptyDesc.textContent = "Try lowering the minimum value filter.";
					emptyState.classList.remove('hidden');
					return;
				}

				emptyState.classList.add('hidden');
				bestsellersContainer.classList.remove('hidden');

				bestsellersBody.innerHTML = items.map((item, idx) => {
					let rankColor = 'text-[var(--color-muted-foreground)]';
					let rankBadge = '';
					if (idx === 0) {
						rankColor = 'text-yellow-400 font-bold drop-shadow-[0_0_8px_rgba(250,204,21,0.8)]';
						rankBadge = '🥇';
					} else if (idx === 1) {
						rankColor = 'text-slate-300 font-bold drop-shadow-[0_0_8px_rgba(203,213,225,0.8)]';
						rankBadge = '🥈';
					} else if (idx === 2) {
						rankColor = 'text-amber-600 font-bold drop-shadow-[0_0_8px_rgba(217,119,6,0.8)]';
						rankBadge = '🥉';
					}

					const safeName = escapeHtml(item.name);
					const ratePercent = Math.min(100, Math.round(item.saleRate * 100));

					return `
						<tr class="hover:bg-[var(--color-muted)] transition-all duration-200 group hover:shadow-[0_0_15px_rgba(245,158,11,0.15)]">
							<td class="px-6 py-4 text-center">
								<span class="font-heading text-sm ${rankColor}">${rankBadge || `#${idx + 1}`}</span>
							</td>
							<td class="px-6 py-4">
								<div class="flex items-center gap-3">
									${item.icon 
										? `<img src="${item.icon}" alt="${safeName}" class="w-10 h-10 rounded border border-amber-500/30 shadow-sm group-hover:border-amber-400 group-hover:shadow-[0_0_10px_rgba(245,158,11,0.5)] transition-all shrink-0 object-cover" onerror="this.onerror=null; this.src='https://render.worldofwarcraft.com/us/icons/56/inv_misc_questionmark.jpg';" />` 
										: `<div class="w-10 h-10 bg-[var(--color-background)] rounded border border-[var(--color-border)] flex items-center justify-center shrink-0"><span class="text-xs text-[var(--color-muted-foreground)]">?</span></div>`
									}
									<div class="flex flex-col min-w-0">
										<span class="font-heading tracking-wide text-[var(--color-foreground)] group-hover:text-amber-400 transition-colors cursor-pointer" onclick="scanBestSeller('${safeName}')">${safeName}</span>
										<div class="flex items-center gap-2 mt-0.5">
											<span class="text-xs text-[var(--color-muted-foreground)]">${item.expansion}</span>
											<span class="text-[10px] text-[var(--color-muted-foreground)]/60 font-mono">ID: ${item.itemId}</span>
										</div>
									</div>
								</div>
							</td>
							<td class="px-6 py-4">
								<div class="flex flex-col">
									<span class="text-yellow-400 font-heading tracking-wide drop-shadow-[0_0_5px_rgba(250,204,21,0.4)]">${item.marketValue.toLocaleString()}g</span>
									<span class="text-xs text-[var(--color-muted-foreground)] mt-0.5">Avg: ${item.avgSalePrice.toLocaleString()}g</span>
								</div>
							</td>
							<td class="px-6 py-4">
								<span class="font-heading text-slate-200 tracking-wide">${formatCompactNumber(item.soldPerDay)}</span>
								<span class="text-xs text-[var(--color-muted-foreground)] block">units / day</span>
							</td>
							<td class="px-6 py-4">
								<div class="flex items-center gap-2.5">
									<div class="w-20 bg-[var(--color-muted)] rounded-full h-2 overflow-hidden border border-[var(--color-border)]">
										<div class="h-full rounded-full bg-gradient-to-r from-purple-500 to-amber-400" style="width: ${ratePercent}%"></div>
									</div>
									<span class="text-xs font-heading ${ratePercent >= 50 ? 'text-emerald-400' : 'text-amber-400'}">${ratePercent}%</span>
								</div>
							</td>
							<td class="px-6 py-4">
								<span class="font-heading text-amber-400 tracking-wide drop-shadow-[0_0_8px_rgba(245,158,11,0.4)]">
									~${formatCompactNumber(item.dailyTurnoverGold)}g
								</span>
								<span class="text-xs text-[var(--color-muted-foreground)] block">daily turnover</span>
							</td>
							<td class="px-6 py-4 text-right">
								<button onclick="scanBestSeller('${safeName}')" class="px-3.5 py-1.5 text-xs font-heading uppercase tracking-wider bg-[var(--color-primary)] hover:bg-[var(--color-primary)]/80 text-[var(--color-on-primary)] rounded transition-all shadow-[0_0_10px_rgba(124,58,237,0.5)] hover:shadow-[0_0_15px_rgba(124,58,237,0.8)] inline-flex items-center gap-1.5 cursor-pointer">
									<span>⚡</span> Scan Realms
								</button>
							</td>
						</tr>
					`;
				}).join('');
			}

			window.scanBestSeller = function(name) {
				searchInput.value = name;
				fetchAuctions();
			};

			bestsellersBtn?.addEventListener('click', fetchBestSellers);
			bsSortSelect?.addEventListener('change', () => {
				if (isBestSellersView) fetchBestSellers();
			});
			bsMinPriceSelect?.addEventListener('change', () => {
				if (isBestSellersView) fetchBestSellers();
			});

			async function fetchFavorites() {
				setActiveView('favorites');
				if (favorites.length === 0) {
					auctions = [];
					renderTable();
					emptyTitle.textContent = "No Favorites Yet";
					emptyDesc.textContent = "Star some items in your searches to see them here.";
					emptyState.classList.remove('hidden');
					return;
				}

				tableContainer.classList.add('hidden');
				emptyState.classList.add('hidden');
				loading.classList.remove('hidden');
				loading.classList.add('flex');

				try {
					const uniqueBaseIds = [...new Set(favorites.map(f => f.startsWith('pet-') ? f.split('-').slice(0, 2).join('-') : f.split('-')[0]))].join(',');
					const realm = realmSelect.value;
					const expansion = expansionSelect.value;
					
					const response = await fetch(`/api/auctions?ids=${uniqueBaseIds}&realm=${realm}&expansion=${expansion}`);
					const data = await response.json();
					
					// Filter to strictly show the specific variant IDs they favorited
					auctions = data.filter(a => favorites.includes(a.id));
					renderTable();
				} catch (error) {
					console.error('Failed to fetch favorites:', error);
				} finally {
					loading.classList.remove('flex');
					loading.classList.add('hidden');
				}
			}

			favoritesBtn?.addEventListener('click', fetchFavorites);

			async function fetchAuctions() {
				setActiveView('search');
				const query = searchInput.value.trim();
				const realm = realmSelect.value;
				const expansion = expansionSelect.value;
				if (!query) return;

				emptyState.classList.add('hidden');
				tableContainer.classList.add('hidden');
				errorMsg.classList.add('hidden');
				loading.classList.remove('hidden');
				loading.classList.add('flex');

				try {
					const response = await fetch('/api/auctions?q=' + encodeURIComponent(query) + '&realm=' + realm + '&expansion=' + encodeURIComponent(expansion));
					const data = await response.json();

					if (!response.ok) {
						throw new Error(data.error || 'Network response was not ok');
					}
					
					auctions = data;
					renderTable();
				} catch (err) {
					errorDesc.textContent = err.message;
					errorMsg.classList.remove('hidden');
					console.error("Failed to fetch auctions", err);
				} finally {
					loading.classList.add('hidden');
					loading.classList.remove('flex');
				}
			}

			const suggestionsBox = document.getElementById('suggestions');
			let activeSuggestionIdx = -1;

			const QUALITY_COLORS = {
				poor: '#94a3b8',
				common: '#f8fafc',
				uncommon: '#22c55e',
				rare: '#3b82f6',
				epic: '#a855f7',
				legendary: '#f97316',
				artifact: '#eab308',
				heirloom: '#06b6d4'
			};

			function escapeHtml(str) {
				if (!str) return '';
				return String(str)
					.replace(/&/g, '&amp;')
					.replace(/</g, '&lt;')
					.replace(/>/g, '&gt;')
					.replace(/"/g, '&quot;')
					.replace(/'/g, '&#039;');
			}

			function updateSuggestionHighlight() {
				const items = suggestionsBox.querySelectorAll('.suggestion-item');
				items.forEach((item, idx) => {
					if (idx === activeSuggestionIdx) {
						item.classList.add('bg-[var(--color-muted)]', 'ring-1', 'ring-inset', 'ring-[var(--color-primary)]');
						item.scrollIntoView({ block: 'nearest' });
					} else {
						item.classList.remove('bg-[var(--color-muted)]', 'ring-1', 'ring-inset', 'ring-[var(--color-primary)]');
					}
				});
			}

			async function fetchSuggestions(query) {
				if (!query || query.length < 3) {
					suggestionsBox.classList.add('hidden');
					return;
				}
				try {
					const response = await fetch('/api/suggestions?q=' + encodeURIComponent(query));
					const data = await response.json();
					if (data && data.length > 0) {
						activeSuggestionIdx = -1;
						suggestionsBox.innerHTML = data.map(s => {
							const qualityColor = (s.quality && QUALITY_COLORS[s.quality]) || 'var(--color-foreground)';
							const safeName = escapeHtml(s.name);
							return `
								<li class="suggestion-item flex items-center gap-3 px-3.5 py-2.5 hover:bg-[var(--color-muted)] cursor-pointer transition-all duration-150 group select-none" data-name="${encodeURIComponent(s.name)}">
									${s.icon 
										? `<img src="${s.icon}" alt="${safeName}" class="w-8 h-8 rounded shrink-0 object-cover shadow-sm transition-all duration-200 group-hover:scale-105 group-hover:shadow-[0_0_8px_rgba(124,58,237,0.5)]" style="border: 1.5px solid ${qualityColor};" loading="lazy" onerror="this.onerror=null; this.src='https://render.worldofwarcraft.com/us/icons/56/inv_misc_questionmark.jpg';" />` 
										: `<div class="w-8 h-8 rounded border border-[var(--color-border)] bg-[var(--color-background)] flex items-center justify-center shrink-0 text-xs text-[var(--color-muted-foreground)]">?</div>`
									}
									<div class="flex items-center justify-between min-w-0 flex-1 gap-2">
										<span class="text-sm font-heading tracking-wide transition-colors truncate" style="color: ${qualityColor};">${safeName}</span>
										${s.isPet ? `<span class="text-[10px] font-heading uppercase tracking-wider text-cyan-400 bg-cyan-950/60 px-1.5 py-0.5 rounded border border-cyan-800/50 shrink-0">Pet</span>` : ''}
									</div>
								</li>
							`;
						}).join('');
						suggestionsBox.classList.remove('hidden');
					} else {
						suggestionsBox.classList.add('hidden');
					}
				} catch (err) {
					console.error(err);
				}
			}

			window.selectSuggestion = function(name) {
				searchInput.value = name;
				suggestionsBox.classList.add('hidden');
				activeSuggestionIdx = -1;
				fetchAuctions();
			};

			suggestionsBox.addEventListener('click', (e) => {
				const item = e.target.closest('.suggestion-item');
				if (item && item.dataset.name) {
					selectSuggestion(decodeURIComponent(item.dataset.name));
				}
			});

			document.addEventListener('click', (e) => {
				if (!suggestionsBox.contains(e.target) && e.target !== searchInput) {
					suggestionsBox.classList.add('hidden');
					activeSuggestionIdx = -1;
				}
			});

			searchBtn.addEventListener('click', () => {
				suggestionsBox.classList.add('hidden');
				fetchAuctions();
			});
			
			searchInput.addEventListener('keydown', (e) => {
				if (suggestionsBox.classList.contains('hidden')) {
					if (e.key === 'Enter') {
						fetchAuctions();
					}
					return;
				}

				const items = suggestionsBox.querySelectorAll('.suggestion-item');
				if (e.key === 'ArrowDown') {
					e.preventDefault();
					if (items.length > 0) {
						activeSuggestionIdx = (activeSuggestionIdx + 1) % items.length;
						updateSuggestionHighlight();
					}
				} else if (e.key === 'ArrowUp') {
					e.preventDefault();
					if (items.length > 0) {
						activeSuggestionIdx = (activeSuggestionIdx - 1 + items.length) % items.length;
						updateSuggestionHighlight();
					}
				} else if (e.key === 'Enter') {
					e.preventDefault();
					if (activeSuggestionIdx >= 0 && items[activeSuggestionIdx]) {
						const name = decodeURIComponent(items[activeSuggestionIdx].dataset.name || '');
						if (name) {
							selectSuggestion(name);
							return;
						}
					}
					suggestionsBox.classList.add('hidden');
					fetchAuctions();
				} else if (e.key === 'Escape') {
					suggestionsBox.classList.add('hidden');
					activeSuggestionIdx = -1;
				}
			});

			let debounceTimer;
			searchInput.addEventListener('input', () => {
				const val = searchInput.value.trim();
				clearTimeout(debounceTimer);
				if (val.length === 0) {
					auctions = [];
					emptyTitle.textContent = "Search for a BoE item";
					emptyDesc.textContent = "Enter an item name to scan across multiple realms.";
					renderTable();
					suggestionsBox.classList.add('hidden');
					return;
				}
				debounceTimer = setTimeout(() => {
					if (val.length >= 3) {
						fetchSuggestions(val);
					}
				}, 400); // 400ms debounce for suggestions
			});

			realmSelect.addEventListener('change', () => {
				if (searchInput.value.trim().length >= 3) {
					suggestionsBox.classList.add('hidden');
					fetchAuctions();
				}
			});

			expansionSelect.addEventListener('change', () => {
				if (searchInput.value.trim().length >= 3) {
					suggestionsBox.classList.add('hidden');
					fetchAuctions();
				}
			});
		</script>
	</body>
</html>
